import { useState } from "react";

import { Button } from "@mantine/core";

export default function Navbar() {
  return (
    <div
      style={{ boxShadow: "0px 4px 4px 0px rgba(0, 0, 0, 0.25)" }}
      className="min-h-14 drop-shadow-2xl px-6 rounded-tl-3xl inline-flex flex-wrap items-center z-50 bg-white font-semibold font-sf-pro text-gray-800 text-[18px]"
    >
      1234
    </div>
  );
}
